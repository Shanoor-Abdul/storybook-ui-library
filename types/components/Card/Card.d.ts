import type { CardProps } from "./Card.types";
declare const Card: ({ title, subtitle, description, footer, variant, shadow, className, }: CardProps) => import("react").JSX.Element;
export default Card;
