import type { InputProps } from "./Input.types";
declare const Input: ({ id, label, value, type, placeholder, onChange, helperText, disabled, error, required, className, inputClassName, labelClassName, }: InputProps) => import("react").JSX.Element;
export default Input;
