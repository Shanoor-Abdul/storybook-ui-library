import type { CheckboxProps } from "./Checkbox.types";
declare const Checkbox: ({ id, label, checked, onChange, variant, size, disabled, error, required, }: CheckboxProps) => import("react").JSX.Element;
export default Checkbox;
